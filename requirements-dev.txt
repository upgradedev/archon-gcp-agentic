# Dev and CI tooling. The suite is offline: no key, no credential, no paid call.
-r requirements.txt
pytest>=8.0
pytest-cov>=5.0
httpx>=0.27          # fastapi's TestClient transport
ruff>=0.8            # lint, and the only style gate
pip-audit>=2.7       # dependency CVEs
