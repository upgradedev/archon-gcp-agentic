node_modules/
.venv/
__pycache__/
.env
*.log
dist/
build/
.pytest_cache/
.ruff_cache/
.coverage
coverage.xml
htmlcov/
readiness.json

# Terraform working directory. The provider binary is 125 MB and belongs in a
# release, never in a clone. The lock file IS committed, because it pins the
# provider version and that is part of the declaration.
infra/.terraform/
*.tfstate
*.tfstate.*

# Editor and agent scratch. A launch config is one machine's way of starting the
# server; the deployment is infra/main.tf and the local command is in the README.
.claude/
