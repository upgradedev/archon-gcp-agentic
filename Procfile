# Read by Railway, Render and Heroku. Vercel ignores this file entirely and
# uses [tool.vercel] in pyproject.toml instead; both point at the same shim.
web: uvicorn service.main:app --host 0.0.0.0 --port $PORT
